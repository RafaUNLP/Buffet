package persistencia.clases.entidades;

public class Rol {

	/*el equals va a ser por nombre llevado a lowerCase y sin espacios, para que no importe si son o no la misma ref de memoria
	 * aunque Hibernate deberia hacer que lo sean*/
}
